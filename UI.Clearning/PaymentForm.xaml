<Window x:Class="UI.Cleaning.PaymentForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оплата заявки - Клининг сервис"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Оплата заявки" FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Номер карты:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="CardNumberTextBox" Height="25" Margin="0,20,0,5" 
                 MaxLength="19" TextChanged="CardNumberTextBox_TextChanged"/>

        <TextBlock Grid.Row="2" Text="Срок действия (ММ/ГГ):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="ExpiryDateTextBox" Height="25" Margin="0,20,0,5" 
                 MaxLength="5" TextChanged="ExpiryDateTextBox_TextChanged"/>

        <TextBlock Grid.Row="3" Text="CVV:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="CvvTextBox" Height="25" Margin="0,20,0,5" 
                 MaxLength="3"/>

        <TextBlock Grid.Row="4" Text="Сумма к оплате:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="4" x:Name="AmountTextBlock" FontSize="16" FontWeight="Bold" 
                   Foreground="Green" Margin="0,20,0,5" Text="0 руб."/>

        <Border Grid.Row="5" BorderBrush="LightGray" BorderThickness="1" Margin="0,20,0,10" 
                Background="WhiteSmoke" Padding="10">
            <TextBlock x:Name="RequestDetailsTextBlock" TextWrapping="Wrap"/>
        </Border>

        <Button Grid.Row="7" Content="Оплатить" Click="PayButton_Click" 
                Height="35" FontSize="14" Margin="0,10,0,5"/>
        <Button Grid.Row="7" Content="Назад" Click="BackButton_Click" 
                Height="30" Margin="0,90,0,5"/>
    </Grid>
</Window>