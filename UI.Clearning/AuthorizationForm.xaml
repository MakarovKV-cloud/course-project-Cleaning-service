<Window x:Class="UI.Cleaning.AuthorizationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Клининг сервис"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Логин:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="LoginTextBox" Height="25" Margin="0,20,0,5"/>

        <TextBlock Grid.Row="2" Text="Пароль:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="2" x:Name="PasswordBox" Height="25" Margin="0,20,0,5"/>

        <Button Grid.Row="3" Content="Войти" Click="LoginButton_Click" 
                Height="30" Margin="0,10,0,5"/>
        <Button Grid.Row="3" Content="Создать аккаунт" Click="CreateAccountButton_Click" 
                Height="30" Margin="0,85,0,5"/>
    </Grid>
</Window>